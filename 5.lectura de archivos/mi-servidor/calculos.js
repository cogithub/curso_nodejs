// Definimos una función para sumar dos números
function sumar(num1, num2) {
    let suma = num1 + num2;

    return suma;
}

// Llamamos la función con dos números de ejemplo

console.log(`La suma es: ${sumar(a,b)}`);