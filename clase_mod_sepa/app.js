// Importamos la clase MiClase desde el archivo MiClase.js
import { MiClase } from './MiClase.js';

// Creamos una nueva instancia de MiClase
const mc = new MiClase();

// Llamamos al método confirmar de la instancia
mc.confirmar();
